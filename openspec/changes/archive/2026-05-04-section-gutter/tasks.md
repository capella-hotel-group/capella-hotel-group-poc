## 1. Content Model

- [x] 1.1 Add `gutter` select field to `src/models/_section.json` with options: none, sm, md, lg
- [x] 1.2 Run `npm run build:json` to sync into `component-models.json`

## 2. CSS

- [x] 2.1 Add `[data-gutter]` CSS rules to `src/styles/styles.css` for none / sm / md / lg
