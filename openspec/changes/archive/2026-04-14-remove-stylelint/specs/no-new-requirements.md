# Specs — remove-stylelint

No new behavioral requirements are introduced by this change.
This is a pure tooling removal (dependency and configuration cleanup only).
All CSS formatting is delegated to Prettier, which is already in place.
