## 1. Shared Utility — PointerVelocityTracker

- [ ] 1.1 Create `src/utils/pointer-velocity.ts` and export `PointerVelocityTracker` class with `attach(element)`, `detach()`, `update()`, and `smoothDelta` property
- [ ] 1.2 Implement constructor options: `decayRate` (default `0.032`) and `decayThreshold` (default `0.0001`)
- [ ] 1.3 Implement `pointermove` listener that accumulates NDC delta into `pendingDelta`
- [ ] 1.4 Implement `update()` applying exponential decay formula and zero-snap threshold
- [ ] 1.5 Verify `detach()` is idempotent (safe to call when nothing is attached)

## 2. Refactor lighting-interaction to use PointerVelocityTracker

- [ ] 2.1 In `scene.ts`, replace inline `pendingDelta`, `smoothDelta`, `lastNDC`, and `boundPointerMove` variables with a `PointerVelocityTracker` instance
- [ ] 2.2 Pass `debugConfig.velocityDecayRate` and `debugConfig.velocityDecayThreshold` as tracker constructor options (with module-constant fallbacks)
- [ ] 2.3 Call `tracker.attach(container)` in `initScene` and `tracker.detach()` in `cleanupScene`
- [ ] 2.4 Replace all reads of `smoothDelta` in the animation loop with `tracker.smoothDelta`; call `tracker.update()` at start of each frame
- [ ] 2.5 Remove the now-redundant `boundPointerMove` / `boundContainer` module-level variables (only if they are no longer used)
- [ ] 2.6 Build the project (`npm run build`) and confirm zero TypeScript errors

## 3. panding-gallery — Phase 1 (Static Grid)

- [ ] 3.1 Parse up to 20 image rows from block DOM; extract `<picture>` / `<img>` elements and wrap with `moveInstrumentation`
- [ ] 3.2 Create 5 `div.panding-gallery-column` elements; distribute images column-major (image `i` → column `i % 5`)
- [ ] 3.3 Wrap columns in `div.panding-gallery-columns` container and call `block.replaceChildren(...)` — phase 1 complete and synchronous
- [ ] 3.4 Implement `ResizeObserver` that computes column width = `(containerWidth - gap * 4) / 2.8` and sets a CSS custom property `--pg-col-width` + `width` inline style on each column

## 4. panding-gallery — AEM Model Update

- [ ] 4.1 Add `inputMode` text field (default `"scroll"`, accepts `"scroll"` | `"pointer"`) to `_panding-gallery.json` top-level block model
- [ ] 4.2 Verify `component-models.json` is regenerated by `npm run build:json` with the new field

## 5. panding-gallery — Scroll-Motion Module

- [ ] 5.1 Create `src/blocks/panding-gallery/scroll-motion.ts` exporting a `ScrollMotionController` class
- [ ] 5.2 Constructor accepts: `columns: HTMLElement[]`, `inputMode: 'scroll' | 'pointer'`, `columnWeights?: number[]` (default `[1.0, 0.7, 1.2, 0.85, 1.1]`), `swapPadding?: number` (default `200`)
- [ ] 5.3 Implement wheel-input mode: attach `wheel` event listener on the block element; normalise `deltaX`/`deltaY` per `deltaMode`; prevent default to stop page scroll
- [ ] 5.4 Implement pointer-input mode: instantiate `PointerVelocityTracker`, attach to block; read `tracker.smoothDelta` each frame; call `tracker.update()` in RAF loop
- [ ] 5.5 Implement RAF loop: each frame, accumulate `globalOffsetX += inputDeltaX` and `offsetY[col] += inputDeltaY * columnWeights[col]` for each column
- [ ] 5.6 Apply offsets as `transform: translateX(${globalOffsetX}px)` per column and additional per-column `translateY(${offsetY[col]}px)` on the column element
- [ ] 5.7 Implement Y infinity loop: after updating `offsetY[col]`, check each item's effective screen Y; if below `-swapPadding` teleport by `+totalColumnHeight`, if above `viewportH + swapPadding` teleport by `-totalColumnHeight`
- [ ] 5.8 Implement X infinity loop: check each column's effective screen X; if below `-swapPadding` teleport by `+totalGridWidth`, if above `viewportW + swapPadding` teleport by `-totalGridWidth`
- [ ] 5.9 Compute `totalColumnHeight` as sum of item heights + gaps for each column (snapshot after phase 1, update on resize)
- [ ] 5.10 Expose `cleanup()` method: cancel RAF, remove event listeners, detach velocity tracker (if pointer mode)
- [ ] 5.11 Wire `ResizeObserver` inside the controller to recompute column width, `totalColumnHeight`, and `totalGridWidth` on block resize; pause and resume RAF across the sync recalculation

## 6. panding-gallery — Phase 2 Wiring in decorate()

- [ ] 6.1 Read `inputMode` from block's second row text content (or default `"scroll"`) after phase 1 DOM construction
- [ ] 6.2 Add click listener on block that: sets `initialized = true`, adds `panding-gallery--loading`, dynamically imports `./scroll-motion`
- [ ] 6.3 On successful import: instantiate `ScrollMotionController` with columns and config, remove `--loading`, add `--active`
- [ ] 6.4 On import failure: `console.error`, remove `--loading`, reset `initialized = false` to allow retry

## 7. panding-gallery — CSS

- [ ] 7.1 Add CSS rule for `--pg-col-width` custom property applied to `.panding-gallery-column` width
- [ ] 7.2 Set `aspect-ratio: 3 / 4` and `object-fit: cover` on `.panding-gallery-column img`
- [ ] 7.3 Add `panding-gallery--loading` and `panding-gallery--active` state classes with appropriate cursor/opacity transitions
- [ ] 7.4 Ensure `.panding-gallery-columns` uses `position: absolute; overflow: visible` so columns can scroll beyond viewport without clipping

## 8. Verification

- [ ] 8.1 Run `npm run build` with no TypeScript errors
- [ ] 8.2 Run `npm run lint` with no ESLint errors
- [ ] 8.3 Manually verify: static grid visible before click; scroll wheel moves grid bidirectionally; columns scroll at different vertical speeds; no visible seam during infinity-loop teleportation
- [ ] 8.4 Manually verify `lighting-interaction` block still works identically after pointer-velocity refactor
